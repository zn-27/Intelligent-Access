# 安全认证流程 — 详细文字版

## 参与者

| ID | 角色 | 说明 |
|----|------|------|
| TM | TERMINAL | 移动节点，双网卡(STA+AdHoc)，发起IP请求方。需要同时维护两份共享密钥 |
| AP | AP_SERVER | 域A/B的AP服务器，拥有Chebyshev密钥对，支持密钥分发+HMAC签名验证 |
| GW | GATEWAY | 域C自组织网关，拥有Chebyshev密钥对，负责密钥分发+IP分配+HMAC签名 |
| CTRL | SDN控制器 | 接收ARP表项下发 |

**关键区别**: 终端需要同时维护两份共享密钥:
- `m_sharedSecretAp`: 与当前域AP_SERVER协商的共享密钥 (域A/B使用)
- `m_sharedSecretGw`: 与GATEWAY协商的共享密钥 (域C使用)，跨域保留不清除

## 加密参数

| 参数 | 值 |
|------|-----|
| 密钥协商算法 | Extended Chebyshev 多项式 (基于混沌映射) |
| 模数 | 128位随机素数 |
| 基点 | x = 2 |
| 共享密钥长度 | 16字节 (128位, AES-128级别) |
| 签名算法 | HMAC-SHA256，取前64位(8字节)，hex编码为16字符 |
| 密钥交换触发条件(AP路径) | 终端收到 TYPE:AP_CRYPTO 且 SEC:CHEBYSHEV |
| 密钥交换触发条件(AdHoc路径) | 终端收到 TYPE:IBSS_BEACON 且 HOPS_TO_GW==0 且 SEC:CHEBYSHEV |
| AP共享密钥生命周期 | 切换AP域时重新协商 |
| GW共享密钥生命周期 | 一旦建立，跨域切换不清除 |
| 密钥交换触发条件 | 终端收到 HOPS_TO_GW==0 且 SEC:CHEBYSHEV 的伪信标 |
| 密钥跨域保留 | 是，共享密钥一旦建立，跨域切换不清除 |

---

## 流程一: 左侧入网 (域A/B, AP主导, 完整加密认证)

### Phase 0: AP_SERVER 启动初始化

#### AP_SERVER 加密初始化 (StartApplication)
- 调用 InitCrypto() → m_cryptoEnabled = true
- 调用 GenerateChebyshevKeypair():
  - 生成128位随机素数 MODULUS
  - 基于MODULUS创建 ExtendedChebyshevKeyExchange 实例
  - 生成私钥 m_privateKeyBytes
  - 以基点 x=2 计算公钥 m_publicKeyBytes
  - 提取 MODULUS hex 字符串 m_modulusHex

#### AP_SERVER 周期性广播AP_CRYPTO信标 (每2秒)
- 调用 SendApCryptoBeacon():
  a. 检查 m_role == ROLE_AP_SERVER && m_staDevice 存在
  b. 构造消息:
     TYPE:AP_CRYPTO
     SSID:{m_currentSsid}
     SEC:CHEBYSHEV
     MODULUS:{GetModulusHex()}
     PUBKEY:{GetPublicKeyHex()}
  c. 去掉末尾分号
  d. 如果已有共享密钥: 调用 SignMessageAp(payload) 追加 HMAC:{hex}
     (注: AP_SERVER使用 m_sharedSecret 存储与终端的共享密钥)
  e. 通过 m_staDevice->Send(p, broadcast, 0x0800) 直发
  f. 调度下次广播: 2秒后

#### TERMINAL 初始化
- 调用 InitCrypto() → m_cryptoEnabled = true
- 尚未生成密钥对, m_sharedSecretAp = nullptr

---

### Phase 1: 链路发现 + AP密钥协商

#### Step 1.1: AP_SERVER 广播标准Beacon + AP_CRYPTO信标
- 标准802.11 Beacon帧: 携带 SSID (A或B), SNR, BSS Load, RSN IE
- AP_CRYPTO信标(原始包): 携带 SEC:CHEBYSHEV + MODULUS + PUBKEY
- 两套信标独立发送，互不干扰

#### Step 1.2: 终端 STA Sniffer 接收并处理AP_CRYPTO
- 终端 ReceiveStaBeacon() 中的非Beacon帧处理分支:
  a. 检测 payload 包含 TYPE:AP_CRYPTO
  b. 检查 m_role == ROLE_TERMINAL && m_cryptoEnabled
  c. ExtractField("MODULUS") → modHex
  d. ExtractField("PUBKEY") → pubHex
  e. 条件: modHex和pubHex均非空 && m_sharedSecretAp 为 nullptr
  f. 调用 ComputeSharedSecretFromAp(modHex, pubHex):
     - 将 modHex 转为 mpz_class 素数
     - 创建 ExtendedChebyshevKeyExchange 实例
     - 生成终端自己的私钥+公钥(基点x=2)
     - 将 pubHex 转为字节数组
     - compute_shared_secret_bytes(终端私钥, AP公钥) → 共享密钥
     - 取前16字节保存到 m_sharedSecretAp (终端AP路径共享密钥)
     - 打印 "终端与AP_SERVER完成Chebyshev密钥协商"
  g. HMAC验证: 如果 m_sharedSecretAp 已建立且信标携带HMAC:
     - ExtractField("HMAC")
     - VerifyMessageAp(msgBody, hmacHex)
     - 失败则打印 "[安全警告] AP_CRYPTO HMAC验证失败!"
  h. return (处理完毕，不继续解析为标准Beacon)

#### Step 1.3: 标准AP Beacon处理 (与加密无关)
- 终端解析标准802.11 Beacon帧
- 提取 SSID, SNR, BSS Load, RSN IE
- 过滤 SSID="C" (域C对终端是自组织域)
- 记录到 m_neighborRadar (TYPE_AP)

---

### Phase 2: 网络评估与切换

- EvaluateAndSwitch() 选出最佳候选域
- 首次入网优先选AP域 (m_currentHops==99 时扫描5轮等AP)
- ExecuteSwitch(AP类型, 域A或B):
  a. 确定目标域ID: A→1, B→2
  b. 域变了则: 释放旧域IP, 重置双网卡IP状态, 清除 m_sharedSecretAp (新AP需要重新协商)
  c. 记录 AP Beacon 时间戳
  d. STA PHY 锁目标信道: A→ch1/2412MHz, B→ch6/2437MHz
  e. staMac->SetSsid(A或B)
  f. SetDataPlaneActive(DATA_PLANE_AP): STA数据激活, AdHoc数据静默
  g. 关闭伪信标广播socket (AP域不需要)
  h. 调度: 3秒后 RequestStaIp(), 0.5秒后 RequestAdhocIp()

### Phase 3: STA链路层关联
- STA自动发起Association Request
- AP回复Association Response (分配AID)
- 终端 RequestStaIp() 检查 IsAssociated()，未关联则1秒后重试

---

### Phase 4: IP_REQUEST (STA路径, 带HMAC签名)

#### Step 4.1: 终端 RequestStaIp()
  a. 创建UDP socket, BindToNetDevice(m_staDevice)
  b. 构造消息:
     TYPE:IP_REQUEST
     MAC:{tm_sta_mac}
     PUBKEY:{GetPublicKeyHex()}  (如果加密已启用且有公钥)
  c. HMAC签名: 如果 m_cryptoEnabled && m_sharedSecretAp:
     - 调用 SignMessageAp(payload):
       - hmacSha256First64Bits(msgBody, m_sharedSecretAp, m_sharedSecretApLen) → 8字节
       - bytesToHex(8字节) → 16字符hex字符串
     - 追加 HMAC:{16字符hex}
  d. 通过 m_staDevice->Send() 直发 (绕过EDCA)
  e. 首次发送时记录 IP_REQUEST 时间戳 (设备标签=STA, 域ID=1或2)

---

### Phase 5: AP_SERVER 处理IP_REQUEST (HMAC验证+签名)

#### Step 5.1: AP_SERVER ReceiveApSniffer()
  a. 检查 m_role == ROLE_AP_SERVER
  b. 检查 TYPE:IP_REQUEST
  c. 提取 MAC 字段
  d. 密钥协商: 如果 m_cryptoEnabled && m_sharedSecret==nullptr:
     - ExtractField("PUBKEY") → termPubHex
     - 如果 termPubHex 非空且有私钥:
       - hex_to_bytes(termPubHex) → termPubBytes
       - compute_shared_secret_bytes(AP私钥, 终端公钥) → secret
       - secret长度>=16: 取前16字节保存到 m_sharedSecret (GATEWAY只维护一份共享密钥)
       - 打印 "AP_SERVER与终端完成Chebyshev密钥协商"
  e. HMAC验证: 如果 m_cryptoEnabled && m_sharedSecret:
     - ExtractField("HMAC") → hmacHex
     - 如果 hmacHex 非空:
       - 定位 ;HMAC: 之前的部分作为 msgBody
       - VerifyMessage(msgBody, hmacHex):
         - 本地调用 SignMessage(msgBody) 重算HMAC
         - 比对 expected == hmacHex
       - 如果验证失败: 打印 "[安全警告] AP IP_REQUEST HMAC验证失败!" 并丢弃
  f. 验证通过后:
     - AllocateIp(mac) 从IP池分配: 域A=10.1.1.x, 域B=10.2.1.x
     - 构造 IP_OFFER:
       TYPE:IP_OFFER
       MAC:{mac}
       IP:{allocated_ip}
       MASK:{m_poolMask}
       GW:{m_poolBase}
     - HMAC签名: 如果 m_cryptoEnabled && m_sharedSecret:
       - SignMessage(resp) → hmacHex
       - 追加 HMAC:{hmacHex}
     - 延迟2ms后通过 m_staDevice->Send() 发送
     - 触发 m_ipAllocatedCallback(mac, ip) 通知控制器

---

### Phase 6: 终端接收IP_OFFER (HMAC验证)

#### Step 6.1: 终端 STA Sniffer 接收IP_OFFER
- ReceiveStaBeacon() 非Beacon帧处理:
  a. 检测 TYPE:IP_OFFER
  b. 提取MAC字段，检查 dm == m_staDevice->GetAddress()
  c. 解析 IP, MASK, GW 字段
  d. HMAC验证: 如果 m_cryptoEnabled && m_sharedSecretAp:
     - ExtractField("HMAC") → hmacHex
     - VerifyMessageAp(msgBody, hmacHex):
       - 本地调用 SignMessageAp(msgBody) 重算HMAC
       - 比对 expected == hmacHex
     - 如果验证失败: 打印 "[安全警告] STA IP_OFFER HMAC验证失败!" 并丢弃
  e. 验证通过后继续处理...

#### Step 6.2: 终端处理验证通过的IP_OFFER
  a. 记录 IP_OFFER 时间戳 (设备标签=STA, 域ID=1或2)
  b. ConfigureIpOnInterface(GetSocketStaDev(), ip, mask, gw)
  c. 更新 m_staState: ip, gw, mask
  d. 左侧同步逻辑: m_dataPlaneMode==DATA_PLANE_AP 且域1或2
     → ConfigureAdhocIpByDomain(domainId)
     → 硬编码配置AdHoc网卡: 域A=10.100.1.100, 域B=10.100.2.100
  e. EnsureBothInterfacesUp()
  f. 触发 AddArpEntry 回调 (只记一条STA主导):
     - m_ipAllocatedCallback(STA_MAC, STA_IP)
     - m_ipAllocatedCallback(AdHoc_MAC, AdHoc_IP) (如果AdHoc已配置)
  g. 打印配置成功横幅

---

### Phase 7: 数据面就绪 (同之前)
- SetDataPlaneActive(DATA_PLANE_AP):
  - STA数据面激活, AdHoc数据面静默
  - 默认路由指向STA网关

---

## 流程二: 右侧入网 (域C, AdHoc主导, 完整加密认证)

### Phase 0: 启动初始化
- GATEWAY:
  - InitCrypto() → m_cryptoEnabled = true
  - GenerateChebyshevKeypair():
    - 生成128位随机素数 MODULUS
    - 基于MODULUS创建 ExtendedChebyshevKeyExchange 实例
    - 生成私钥 m_privateKeyBytes
    - 以基点 x=2 计算公钥 m_publicKeyBytes
    - 提取 MODULUS hex 字符串 m_modulusHex
- TERMINAL:
  - InitCrypto() → m_cryptoEnabled = true
  - m_sharedSecretGw = nullptr, m_sharedSecretAp = nullptr
  - 尚未生成密钥对, 等待接收GW密钥材料

### Phase 1: 密钥材料分发 (AdHoc路径, 存储到 m_sharedSecretGw)

#### Step 1.1: GATEWAY 广播伪信标 (每2秒)
- 消息类型: TYPE:IBSS_BEACON
- 构造过程 SendPseudoBeacon():
  a. PurgeNeighborTable(): 清理3秒未见的邻居
  b. 计算 m_localHops (GATEWAY固定为0)
  c. 收集邻居统计: AVG_LOAD, MIN_ENERGY, NODES
  d. 获取AdHoc设备IP作为源IP
  e. 构造消息:
     TYPE:IBSS_BEACON
     SSID:{m_adhocSsid}
     MAC:{m_adhocDevice->GetAddress()}
     IP:{myIp}
     TS:{Simulator::Now()}
     HOPS_TO_GW:{m_localHops}
     NODES:{activeNodes}
     AVG_LOAD:{avgLoad}
     MIN_ENERGY:{domainMinEnergy}
     SEC:CHEBYSHEV
     MODULUS:{GetModulusHex()}
     PUBKEY:{GetPublicKeyHex()}
  f. 去掉末尾分号(保持与接收端msgBody一致)
  g. 如果已有共享密钥: SignMessage(payload) → 追加 HMAC:{hex}
     (注: GATEWAY使用 m_sharedSecret)
  h. 通过 m_broadcastSocket->SendTo() 广播
  i. 调度下次发送: 2秒 + jitter(0-0.1秒)

#### Step 1.2: 终端接收伪信标 → 密钥协商
- 终端 ReceiveAdhocBeacon() 处理:
  a. 解析字段: IP, HOPS_TO_GW, AVG_LOAD, MIN_ENERGY, NODES, SSID
  b. 检查 SEC:CHEBYSHEV (hasCheb=true)
  c. 密钥交换条件: m_role==ROLE_TERMINAL && m_cryptoEnabled && hasCheb && hops==0
  d. 定位应用层消息体(从TYPE:开始)
  e. ExtractField("MODULUS") → modHex
  f. ExtractField("PUBKEY") → pubHex
  g. 条件: modHex和pubHex均非空 && m_sharedSecret为nullptr
  h. 调用 ComputeSharedSecretFromGateway(modHex, pubHex):
     - 将 modHex 转为 mpz_class 素数
     - 清理旧密钥交换实例，用新模数创建
     - 清理旧公私钥，生成新私钥+公钥(基点x=2)
     - 将 pubHex 转为字节数组
     - compute_shared_secret_bytes(私钥, GW公钥) → 共享密钥
     - 取前16字节保存到 m_sharedSecret (GATEWAY只维护一份共享密钥)
     - 打印 "终端与网关完成Chebyshev密钥协商"
  i. HMAC验证: 如果 m_sharedSecret 已建立且beacon携带HMAC:
     - ExtractField("HMAC")
     - 定位消息体(;HMAC:之前部分)
     - VerifyMessage(msgBody, hmacHex)
     - 失败则打印 "[安全警告] 伪信标HMAC验证失败!"
  j. 记录域状态: 更新 m_neighborRadar

### Phase 2: 网络切换

#### Step 2.1: 终端评估切换
- EvaluateAndSwitch() 评估最佳候选
- 域C为Adhoc-C (SSID匹配且类型为TYPE_ADHOC)
- ExecuteSwitch(AdHoc类型, isDomainC=true):
  a. 域变了则释放旧IP、重置双网卡状态
  b. 记录 IBSS_BEACON 时间戳
  c. STA网卡连接C域AP: SetSsid("C"), PHY锁ch11/2462MHz
  d. SetDataPlaneActive(DATA_PLANE_AP) (C域走STA承载业务)
  e. 调度AP域信道重扫
  f. 调度: 3秒后 RequestStaIp(), 0.5秒后 RequestAdhocIp()

### Phase 3: IP_REQUEST (AdHoc路径, 带HMAC签名)

#### Step 3.1: 终端发送IP_REQUEST
- 终端 RequestAdhocIp():
  a. 检查 m_adhocDevice 存在且 m_adhocState.ip 为空
  b. 构造消息:
     TYPE:IP_REQUEST
     MAC:{m_adhocDevice->GetAddress()}
     PUBKEY:{GetPublicKeyHex()}  (如果加密已启用且有公钥)
  c. HMAC签名: 如果 m_cryptoEnabled && m_sharedSecret:
     - SignMessage(payload):
       - hmacSha256First64Bits(msgBody, sharedSecret) → 8字节
       - bytesToHex(8字节) → 16字符hex字符串
     - 追加 HMAC:{16字符hex}
  d. 创建 socket: BindToNetDevice(m_adhocDevice), Bind(port 69)
  e. device直发: m_adhocDevice->Send(packet, broadcast, 0x0800)
  f. 首次发送时记录 IP_REQUEST 时间戳 (设备标签=ADHOC, 域ID=3)

#### Step 3.2: GATEWAY 处理IP_REQUEST → 密钥协商 + 分配IP
- GATEWAY ReceiveAdhocBeacon() 处理:
  a. 检查 TYPE:IP_REQUEST
  b. 检查 m_role == ROLE_GATEWAY
  c. 提取 MAC 字段
  d. 密钥协商: 如果 m_cryptoEnabled && m_sharedSecret为空:
     - ExtractField("PUBKEY") → termPubHex
     - 如果 termPubHex 非空且有私钥:
       - hex_to_bytes(termPubHex) → termPubBytes
       - compute_shared_secret_bytes(私钥, 终端公钥) → secret
       - secret长度>=16: 取前16字节保存到 m_sharedSecret (GATEWAY只维护一份共享密钥)
       - 打印 "网关与终端完成Chebyshev密钥协商"
  e. AllocateIp(mac) 从IP池分配: 10.100.3.10-50
  f. 构造 IP_OFFER:
     TYPE:IP_OFFER
     MAC:{mac}
     IP:{allocated_ip}
     MASK:{m_poolMask}
     GW:{m_poolBase}
  g. HMAC签名: 如果 m_cryptoEnabled && m_sharedSecret:
     - SignMessage(resp) → hmacHex
     - 追加 HMAC:{hmacHex}
  h. 双设备发送:
     - m_adhocDevice->SendFrom(p, addr, broadcast, 0x0800)
     - m_staDevice->Send(p2, broadcast, 0x0800) (同时从AP设备发)
  i. 触发 m_ipAllocatedCallback(mac, ip) 通知控制器

### Phase 4: 终端处理IP_OFFER (HMAC验证)

#### Step 4.1: 终端接收并验证IP_OFFER
- 终端通过STA sniffer收到IP_OFFER后转发至 ReceiveAdhocBeacon():
  a. 检查 TYPE:IP_OFFER && m_role==ROLE_TERMINAL
  b. 提取MAC字段，检查 dm == m_adhocDevice->GetAddress() (只能自己收)
  c. 调用 HandleAdhocIpMessage(payload)

#### Step 4.2: HandleAdhocIpMessage 处理
  a. 解析 IP, MASK, GW 字段
  b. 记录 IP_OFFER 时间戳 (设备标签=ADHOC, 域ID=3)
  c. HMAC验证: 如果 m_cryptoEnabled && m_sharedSecret:
     - 定位应用层消息体(从TYPE:开始)
     - ExtractField("HMAC") → hmacHex
     - 定位 ;HMAC: 位置
     - 提取 msgBody (HMAC之前部分)
     - VerifyMessage(msgBody, hmacHex):
       - 本地调用 SignMessage(msgBody) 重算HMAC
       - 比对 expected == hmacHex
     - 如果验证失败: 打印 "[安全警告] IP_OFFER HMAC验证失败!" 并丢弃
  d. 验证通过后的处理:
     - 保存 m_assignedIp, m_assignedMask, m_assignedGw
     - 更新 m_adhocState: ip, gw, mask
     - 取消重试定时器
     - ConfigureIpOnInterface(GetSocketAdhocDev(), ip, mask, gw)
     - 右侧同步逻辑: m_dataPlaneMode==DATA_PLANE_ADHOC
       → ConfigureStaIpByDomain(3)
       → 硬编码配置STA网卡: 10.3.1.100
     - EnsureBothInterfacesUp()

### Phase 5: IP_CONFIRM + AddArpEntry

#### Step 5.1: 终端发送IP_CONFIRM (延迟2ms)
  - 构造消息:
    TYPE:IP_CONFIRM
    MAC:{m_adhocDevice->GetAddress()}
    IP:{ip}
  - 延迟2ms后通过 SendDelayedIpConfirm() 发送:
    - 如有广播socket: SendTo(broadcast, port 9)
    - 否则: m_adhocDevice->Send(broadcast)
    - 记录 IP_CONFIRM 时间戳 (设备标签=ADHOC, 域ID=3)

#### Step 5.2: 触发AddArpEntry (在IP_CONFIRM发送之后)
  - 在 SendDelayedIpConfirm() 中IP_CONFIRM发送后:
    - 记录 AddArpEntry 时间戳 (设备标签=ADHOC, 域ID=3)
    - m_ipAllocatedCallback(AdHoc_MAC, AdHoc_IP)
    - m_ipAllocatedCallback(STA_MAC, STA_IP) (如果STA已配置)

#### Step 5.3: 数据面就绪
  - SetDataPlaneActive(DATA_PLANE_AP) (C域走STA承载业务数据)
  - 默认路由指向STA网关
  - 打印配置成功横幅 (双网卡IP)

---

## 流程三: 第三次入网 (域B, AP主导, 共享密钥跨域保留)

与流程一完全一致，区别:
- 信标SSID=B, 信道ch6
- STA IP: 10.2.1.x, AdHoc同步IP: 10.100.2.100
- m_sharedSecretGw: 第2次入网与GATEWAY协商的共享密钥仍保留, 跨域不清除
- m_sharedSecretAp: 切换域时清除, 收到AP_SERVER_B的AP_CRYPTO信标后重新协商
- 终端进入域B后如需AdHoc交互, 继续使用 m_sharedSecretGw 签名; STA交互使用新协商的 m_sharedSecretAp

---

## 加密API摘要

| 方法 | 用途 | 输入 | 输出 |
|------|------|------|------|
| InitCrypto() | 启用加密标记 | 无 | m_cryptoEnabled=true |
| GenerateChebyshevKeypair() | 生成密钥对 | 无 | 128位模数+私钥+公钥 |
| ComputeSharedSecretFromGateway(modHex, pubHex) | 终端计算AdHoc共享密钥→m_sharedSecretGw | GW的模数+公钥 | 16字节共享密钥 |
| ComputeSharedSecretFromAp(modHex, pubHex) | 终端计算AP共享密钥→m_sharedSecretAp | AP的模数+公钥 | 16字节共享密钥 |
| compute_shared_secret_bytes(私钥, 对端公钥) | Chebyshev密钥交换核心 | 两对密钥 | 共享密钥 |
| SignMessage(msgBody) | HMAC签名(使用GATEWAY/AP_SERVER的 m_sharedSecret) | 消息体 | 16字符hex |
| SignMessageAp(msgBody) | HMAC签名(使用终端的 m_sharedSecretAp) | 消息体 | 16字符hex |
| VerifyMessage(msgBody, hmacHex) | HMAC验证(使用GATEWAY/AP_SERVER的 m_sharedSecret) | 消息体+签名 | bool |
| VerifyMessageAp(msgBody, hmacHex) | HMAC验证(使用终端的 m_sharedSecretAp) | 消息体+签名 | bool |
| SendApCryptoBeacon() | AP_SERVER周期性广播密钥材料(TYPE:AP_CRYPTO) | 无 | 每2秒广播 |
| GetPublicKeyHex() | 获取本节点公钥hex | 无 | hex字符串 |
| GetModulusHex() | 获取模数hex | 无 | hex字符串 |
| ExtractField(payload, key) | 从payload提取字段值 | 消息+字段名 | 字段值 |

## 共享密钥存储结构

| 节点 | 字段 | 说明 |
|------|------|------|
| GATEWAY | m_sharedSecret | 与终端的共享密钥(一份, 因为GW只处理AdHoc路径) |
| AP_SERVER | m_sharedSecret | 与终端的共享密钥(一份, 因为AP只处理AP路径) |
| TERMINAL | m_sharedSecretGw | 与GATEWAY协商的共享密钥(AdHoc路径, 跨域保留) |
| TERMINAL | m_sharedSecretAp | 与当前AP_SERVER协商的共享密钥(AP路径, 切换AP域时清除重新协商) |

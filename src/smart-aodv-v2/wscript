## -*- Mode: python; py-indent-offset: 4; indent-tabs-mode: nil; coding: utf-8; -*-

def build(bld):
    module = bld.create_ns3_module('smart-aodv-v2', ['internet', 'wifi'])
    module.includes = '.'
    module.source = [
        'model/smart-aodv-v2-id-cache.cc',
        'model/smart-aodv-v2-dpd.cc',
        'model/smart-aodv-v2-rtable.cc',
        'model/smart-aodv-v2-rqueue.cc',
        'model/smart-aodv-v2-packet.cc',
        'model/smart-aodv-v2-neighbor.cc',
        'model/smart-aodv-v2-routing-protocol.cc',
        'helper/smart-aodv-v2-helper.cc',
        ]

    # Temporarily disable tests to focus on core module
    # smart_aodv_v2_test = bld.create_ns3_module_test_library('smart-aodv-v2')
    # smart_aodv_v2_test.source = [
    #     'test/aodv-id-cache-test-suite.cc',
    #     'test/aodv-test-suite.cc',
    #     'test/aodv-regression.cc',
    #     'test/bug-772.cc',
    #     'test/loopback.cc',
    #     ]

    # # Tests encapsulating example programs should be listed here
    # if (bld.env['ENABLE_EXAMPLES']):
    #     smart_aodv_v2_test.source.extend([
    #         #   'test/aodv-examples-test-suite.cc',
    #             ])

    headers = bld(features='ns3header')
    headers.module = 'smart-aodv-v2'
    headers.source = [
        'model/smart-aodv-v2-id-cache.h',
        'model/smart-aodv-v2-dpd.h',
        'model/smart-aodv-v2-rtable.h',
        'model/smart-aodv-v2-rqueue.h',
        'model/smart-aodv-v2-packet.h',
        'model/smart-aodv-v2-neighbor.h',
        'model/smart-aodv-v2-routing-protocol.h',
        'helper/smart-aodv-v2-helper.h',
        ]

    if bld.env['ENABLE_EXAMPLES']:
        bld.recurse('examples')

    # Temporarily disable Python bindings
    # bld.ns3_python_bindings()
